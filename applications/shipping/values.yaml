replicaCount: 1

image:
  repositoryUrl: us-central1-docker.pkg.dev/google-samples/microservices-demo/shippingservice
  tag: v0.10.4
  pullPolicy: IfNotPresent
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051

resources:
  requests:
    cpu: 100m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi

readinessProbe:
  periodSeconds: 5
  grpc:
    port: 50051

livenessProbe:
  periodSeconds: 5
  grpc:
    port: 50051

startupProbe:
  periodSeconds: 5
  failureThreshold: 30
  grpc:
    port: 50051

pvc:
  create: false
configmap:
  create: false
secret:
  create: false

env:
  PORT: "50051"
  DISABLE_PROFILER: "1"
